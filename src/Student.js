class Student {
  constructor(studentId, firstName, lastName, githubName, email) {
    this.studentId = studentId
    this.firstName = firstName
    this.lastName = lastName
    this.githubName = githubName
    this.email = email
  }
}

export default Student
