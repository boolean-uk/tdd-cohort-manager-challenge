class Student {
  constructor(Id, firstName, lastNames, github, email) {
    this.Id = Id
    this.firstName = firstName
    this.lastName = lastNames
    this.github = github
    this.email = email
  }
}

module.exports = Student
