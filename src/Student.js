class Student {
  constructor(ID, firstName, lastName, gitName, email) {
    this.studentID = ID
    this.firstName = firstName
    this.lastName = lastName
    this.gitName = gitName
    this.email = email
  }
}

module.exports = Student
