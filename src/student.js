class Student {
  constructor(firstName, lastName, userName, email, ID) {
    this.firstName = firstName
    this.lastName = lastName
    this.githubUsername = userName
    this.email = email
    this.StudentID = ID
  }
}

module.exports = Student
