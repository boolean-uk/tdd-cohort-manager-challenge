class Student {
  constructor (id, firstName, lastName, githubUsername, email) {
    this.studentID = id
    this.firstName = firstName
    this.lastName = lastName
    this.githubAccount = githubUsername
    this.emailAddress = email
  }
}

module.exports = Student
