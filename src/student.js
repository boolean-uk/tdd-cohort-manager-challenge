class Student {
  constructor(id, firstName, lastName, githubUsername, email) {
    this.studentID = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.githubUser = githubUsername;
    this.email = email;
  }
}

module.exports = Student;
