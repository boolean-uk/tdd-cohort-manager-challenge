import Student from './student.js'
import student from './student.js'

class Cohort {
  constructor(name) {
    this.name = name
    this.studentsNum = 0
    this.studentsList = []
  }
  addStudent(firstName, lastName) {}
}

export default Cohort
