const DATA = require('./students.json');
let COHORTS = [];
console.log(DATA);

class Exception {
  constructor() {}
  //Throw exceptions if student not found
  alertStudentNotFound() {}

  //Throw exceptions if cohort not found
  alertCohortNotFound() {}
}

// cohortOne = new CohortManager();

// cohortOne.createCohortWithName();
