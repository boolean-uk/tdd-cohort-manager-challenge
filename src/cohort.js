class Cohort {
  constructor() {
    this.cohortName = cohortName
    this.studentList = []
  }

  addStudent(student) {
    const newStudent = {
      id,
      firstName,
      lastName,
      githubUsername,
      email
    }
    this.studentList.push(student)
    return this.studentList
  }

  removeStudent() {}
}
module.exports = Cohort
