const Cohortmanager = require('../src/Cohortmanager')

describe('', () => {
  it()
})

// describe('add a Cohort', () => {
//   it('should have a cohort added to it', () => {
//     let cohortList =  Cohort { name: 'cohort 1', studentList: [] }
//     expect(aNewCohort).toEqual(cohortList)
//   })
// })
