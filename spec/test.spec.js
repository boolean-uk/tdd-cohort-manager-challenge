const Manager = require('../src/manager.js')
const Student = require('../src/student.js')
const Corhort = require('../src/cohort.js')


describe('Cohort tests',() => {
    let cohort;

    beforeEach(() => {
        cohort = new Cohort()
        manager = new CohortManager()
        c
    })

})
